import java.util.*;

/**
 * Responsible for forming pods based on
 * the structure of the StudentGraph.
 */
public class PodFormation {

    /**
     * Constructs a new pod-formation helper for the given student graph.
     *
     * @param graph the graph representing students and their pairwise              connection strengths
     */
    public PodFormation(StudentGraph graph) {
        // Constructor
    }

    /**
     * Forms pods of the specified size from the underlying
     * StudentGraph.
     *
     * @param podSize the desired number of students per pod; behavior is                implementation-defined if the total number of                students is not divisible by this size
     */
    public void formPods(int podSize) {
        // Method signature only
    }
}
