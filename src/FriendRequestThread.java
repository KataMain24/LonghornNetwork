/**
 * A runnable task that represents a friend request between two
 * UniversityStudent objects.
 */
public class FriendRequestThread implements Runnable {

    /**
     * Creates a new friend-request task from one student to another.
     *
     * @param sender   the student initiating the friend request
     * @param receiver the student receiving the friend request
     */
    public FriendRequestThread(UniversityStudent sender, UniversityStudent receiver) {
        // Constructor
    }


    /**
     * Executes the friend-request operation.
     */
    @Override
    public void run() {
        // Method signature only
    }
}
